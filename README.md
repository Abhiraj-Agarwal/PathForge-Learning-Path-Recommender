# PathForge

AI-powered personalized learning path recommender.

## Local setup

```bash
python -m venv .venv
python -m pip install -r requirements.txt
```

Copy `.env.example` to `.env` and configure the API and LLM settings before running the application.
